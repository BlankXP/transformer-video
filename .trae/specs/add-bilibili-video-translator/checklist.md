- [x] FastAPI 后端项目可启动，/docs 可访问
- [x] yt-dlp 可成功下载B站视频（通过 URL 或 BV 号）
- [x] ffmpeg 可从视频中提取 WAV 音频（16kHz，单声道）
- [x] 百炼 Paraformer API 可返回带时间戳的语音识别结果
- [x] 长音频（> 5 分钟）自动分段识别并合并结果
- [x] 百炼 Qwen API 可将识别文本翻译为目标语言
- [x] 分批翻译逻辑正确（每批 ≤ 50 行，时间戳对应关系保持）
- [x] SRT 字幕文件格式正确，包含源文本和翻译文本
- [x] ffmpeg 可将 SRT 字幕烧录到视频中生成硬字幕
- [x] WebSocket 实时推送各阶段进度（stage + progress + message）
- [x] POST /api/video/process 可创建处理任务并返回 task_id
- [x] GET /api/video/{task_id}/status 可查询任务状态
- [x] GET /api/subtitle/{task_id}/srt 可下载 SRT 文件
- [x] GET /api/video/{task_id}/download 可下载带硬字幕视频
- [x] PUT /api/subtitle/{task_id} 可保存编辑后的字幕
- [x] POST /api/video/{task_id}/burn 可用编辑后字幕重新烧录视频
- [x] React 前端可正常加载，显示完整页面布局
- [x] VideoInput 组件可输入 URL、选择语言、提交任务
- [x] ProgressPanel 组件通过 WebSocket 实时显示处理进度
- [x] SubtitleEditor 组件可预览和编辑字幕内容
- [x] VideoPlayer 组件可播放视频
- [x] DownloadPanel 组件可下载 SRT 和视频文件
- [x] API 调用失败时自动重试 3 次（指数退避）
- [x] 临时文件存放在 temp/{task_id}/ 目录，有过期清理机制
- [x] 端到端流程完整可用：输入URL→下载→识别→翻译→字幕→烧录→下载
